[project]
name = "hello-op-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "laiba", email = "laiba@retrocausal.ai" }
]
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.0.14",
    "python-dotenv>=1.1.0",
]

[project.scripts]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
